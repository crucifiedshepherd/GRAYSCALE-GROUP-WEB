<footer>
  <p class="loud">FUCK YOUR MOTHERFUCKING CULTURE</p>
  <p class="meta">GRAYSCALE — живём по своим правилам / {new Date().getFullYear()}</p>
  <a class="tg" href="https://t.me/grayscalebrand" target="_blank" rel="noopener">мы в telegram</a>
</footer>

<style>
  footer {
    border-top: 1px solid var(--c-gray-deep);
    padding: var(--s-5) var(--s-4);
    text-align: center;
  }
  .loud {
    font-family: var(--f-caps);
    text-transform: uppercase;
    letter-spacing: .08em;
    margin: 0 0 var(--s-2);
  }
  .meta { font-family: var(--f-mono); color: var(--c-gray); margin: 0 0 var(--s-2); }
  .tg { font-family: var(--f-mono); color: var(--c-gray); }
  .tg:hover { color: var(--c-paper); }
</style>
